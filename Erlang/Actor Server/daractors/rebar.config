{erl_opts, [debug_info]}.
{deps, [jsone,dar_wflib,dar_model,dar_gfslib]}.

{relx, [{release, { daractors, "0.1.0" },
         [daractors,
          sasl,
          dar_wflib,
          dar_model,
          dar_gfslib]},

        {sys_config, "./config/sys.config"},
        {vm_args, "./config/vm.args"},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]
}.

{profiles, [{prod, [{relx, [{dev_mode, false},
                            {include_erts, true}]}]
            }]
}.
